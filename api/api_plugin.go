/*
 * Docker Engine API
 *
 * The Engine API is an HTTP API served by Docker Engine. It is the API the Docker client uses to communicate with the Engine, so everything the Docker client can do can be done with the API.  Most of the client's commands map directly to API endpoints (e.g. `docker ps` is `GET /containers/json`). The notable exception is running containers, which consists of several API calls.  # Errors  The API uses standard HTTP status codes to indicate the success or failure of the API call. The body of the response will be JSON in the following format:  ``` {   \"message\": \"page not found\" } ```  # Versioning  The API is usually changed in each release, so API calls are versioned to ensure that clients don't break. To lock to a specific version of the API, you prefix the URL with its version, for example, call `/v1.30/info` to use the v1.30 version of the `/info` endpoint. If the API version specified in the URL is not supported by the daemon, a HTTP `400 Bad Request` error message is returned.  If you omit the version-prefix, the current version of the API (v1.41) is used. For example, calling `/info` is the same as calling `/v1.41/info`. Using the API without a version-prefix is deprecated and will be removed in a future release.  Engine releases in the near future should support this version of the API, so your client will continue to work even if it is talking to a newer Engine.  The API uses an open schema model, which means server may add extra properties to responses. Likewise, the server will ignore any extra query parameters and request body properties. When you write clients, you need to ignore additional properties in responses to ensure they do not break when talking to newer daemons.   # Authentication  Authentication for registries is handled client side. The client has to send authentication details to various endpoints that need to communicate with registries, such as `POST /images/(name)/push`. These are sent as `X-Registry-Auth` header as a [base64url encoded](https://tools.ietf.org/html/rfc4648#section-5) (JSON) string with the following structure:  ``` {   \"username\": \"string\",   \"password\": \"string\",   \"email\": \"string\",   \"serveraddress\": \"string\" } ```  The `serveraddress` is a domain/IP without a protocol. Throughout this structure, double quotes are required.  If you have already got an identity token from the [`/auth` endpoint](#operation/SystemAuth), you can just pass this instead of credentials:  ``` {   \"identitytoken\": \"9cbaf023786cd7...\" } ``` 
 *
 * API version: 1.41
 * Generated by: api Generator (https://api-generator.tech)
 */

package api

import (
	"encoding/json"
	"net/http"
	"strings"

	"github.com/gorilla/mux"
)

// A PluginApiController binds http requests to an api service and writes the service results to the http response
type PluginApiController struct {
	service PluginApiServicer
}

// NewPluginApiController creates a default api controller
func NewPluginApiController(s PluginApiServicer) Router {
	return &PluginApiController{ service: s }
}

// Routes returns all of the api route for the PluginApiController
func (c *PluginApiController) Routes() Routes {
	return Routes{ 
		{
			"GetPluginPrivileges",
			strings.ToUpper("Get"),
			"/v1.41/plugins/privileges",
			c.GetPluginPrivileges,
		},
		{
			"PluginCreate",
			strings.ToUpper("Post"),
			"/v1.41/plugins/create",
			c.PluginCreate,
		},
		{
			"PluginDelete",
			strings.ToUpper("Delete"),
			"/v1.41/plugins/{name}",
			c.PluginDelete,
		},
		{
			"PluginDisable",
			strings.ToUpper("Post"),
			"/v1.41/plugins/{name}/disable",
			c.PluginDisable,
		},
		{
			"PluginEnable",
			strings.ToUpper("Post"),
			"/v1.41/plugins/{name}/enable",
			c.PluginEnable,
		},
		{
			"PluginInspect",
			strings.ToUpper("Get"),
			"/v1.41/plugins/{name}/json",
			c.PluginInspect,
		},
		{
			"PluginList",
			strings.ToUpper("Get"),
			"/v1.41/plugins",
			c.PluginList,
		},
		{
			"PluginPull",
			strings.ToUpper("Post"),
			"/v1.41/plugins/pull",
			c.PluginPull,
		},
		{
			"PluginPush",
			strings.ToUpper("Post"),
			"/v1.41/plugins/{name}/push",
			c.PluginPush,
		},
		{
			"PluginSet",
			strings.ToUpper("Post"),
			"/v1.41/plugins/{name}/set",
			c.PluginSet,
		},
		{
			"PluginUpgrade",
			strings.ToUpper("Post"),
			"/v1.41/plugins/{name}/upgrade",
			c.PluginUpgrade,
		},
	}
}

// GetPluginPrivileges - Get plugin privileges
func (c *PluginApiController) GetPluginPrivileges(w http.ResponseWriter, r *http.Request) { 
	query := r.URL.Query()
	remote := query.Get("remote")
	result, err := c.service.GetPluginPrivileges(r.Context(), remote)
	//If an error occured, encode the error with the status code
	if err != nil {
		EncodeJSONResponse(err.Error(), &result.Code, w)
		return
	}
	//If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
	
}

// PluginCreate - Create a plugin
func (c *PluginApiController) PluginCreate(w http.ResponseWriter, r *http.Request) { 
	query := r.URL.Query()
	name := query.Get("name")
	tarContext := &*os.File{}
	if err := json.NewDecoder(r.Body).Decode(&tarContext); err != nil {
		w.WriteHeader(http.StatusBadRequest)
		return
	}
	
	result, err := c.service.PluginCreate(r.Context(), name, *tarContext)
	//If an error occured, encode the error with the status code
	if err != nil {
		EncodeJSONResponse(err.Error(), &result.Code, w)
		return
	}
	//If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
	
}

// PluginDelete - Remove a plugin
func (c *PluginApiController) PluginDelete(w http.ResponseWriter, r *http.Request) { 
	params := mux.Vars(r)
	query := r.URL.Query()
	name := params["name"]
	force := query.Get("force")
	result, err := c.service.PluginDelete(r.Context(), name, force)
	//If an error occured, encode the error with the status code
	if err != nil {
		EncodeJSONResponse(err.Error(), &result.Code, w)
		return
	}
	//If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
	
}

// PluginDisable - Disable a plugin
func (c *PluginApiController) PluginDisable(w http.ResponseWriter, r *http.Request) { 
	params := mux.Vars(r)
	name := params["name"]
	result, err := c.service.PluginDisable(r.Context(), name)
	//If an error occured, encode the error with the status code
	if err != nil {
		EncodeJSONResponse(err.Error(), &result.Code, w)
		return
	}
	//If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
	
}

// PluginEnable - Enable a plugin
func (c *PluginApiController) PluginEnable(w http.ResponseWriter, r *http.Request) { 
	params := mux.Vars(r)
	query := r.URL.Query()
	name := params["name"]
	timeout, err := parseInt32Parameter(query.Get("timeout"))
	if err != nil {
		w.WriteHeader(http.StatusBadRequest)
		return
	}
	
	result, err := c.service.PluginEnable(r.Context(), name, timeout)
	//If an error occured, encode the error with the status code
	if err != nil {
		EncodeJSONResponse(err.Error(), &result.Code, w)
		return
	}
	//If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
	
}

// PluginInspect - Inspect a plugin
func (c *PluginApiController) PluginInspect(w http.ResponseWriter, r *http.Request) { 
	params := mux.Vars(r)
	name := params["name"]
	result, err := c.service.PluginInspect(r.Context(), name)
	//If an error occured, encode the error with the status code
	if err != nil {
		EncodeJSONResponse(err.Error(), &result.Code, w)
		return
	}
	//If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
	
}

// PluginList - List plugins
func (c *PluginApiController) PluginList(w http.ResponseWriter, r *http.Request) { 
	query := r.URL.Query()
	filters := query.Get("filters")
	result, err := c.service.PluginList(r.Context(), filters)
	//If an error occured, encode the error with the status code
	if err != nil {
		EncodeJSONResponse(err.Error(), &result.Code, w)
		return
	}
	//If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
	
}

// PluginPull - Install a plugin
func (c *PluginApiController) PluginPull(w http.ResponseWriter, r *http.Request) { 
	query := r.URL.Query()
	remote := query.Get("remote")
	name := query.Get("name")
	xRegistryAuth := r.Header.Get("X-Registry-Auth")
	body := &[]InlineObject{}
	if err := json.NewDecoder(r.Body).Decode(&body); err != nil {
		w.WriteHeader(http.StatusBadRequest)
		return
	}
	
	result, err := c.service.PluginPull(r.Context(), remote, name, xRegistryAuth, *body)
	//If an error occured, encode the error with the status code
	if err != nil {
		EncodeJSONResponse(err.Error(), &result.Code, w)
		return
	}
	//If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
	
}

// PluginPush - Push a plugin
func (c *PluginApiController) PluginPush(w http.ResponseWriter, r *http.Request) { 
	params := mux.Vars(r)
	name := params["name"]
	result, err := c.service.PluginPush(r.Context(), name)
	//If an error occured, encode the error with the status code
	if err != nil {
		EncodeJSONResponse(err.Error(), &result.Code, w)
		return
	}
	//If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
	
}

// PluginSet - Configure a plugin
func (c *PluginApiController) PluginSet(w http.ResponseWriter, r *http.Request) { 
	params := mux.Vars(r)
	name := params["name"]
	body := &[]string{}
	if err := json.NewDecoder(r.Body).Decode(&body); err != nil {
		w.WriteHeader(http.StatusBadRequest)
		return
	}
	
	result, err := c.service.PluginSet(r.Context(), name, *body)
	//If an error occured, encode the error with the status code
	if err != nil {
		EncodeJSONResponse(err.Error(), &result.Code, w)
		return
	}
	//If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
	
}

// PluginUpgrade - Upgrade a plugin
func (c *PluginApiController) PluginUpgrade(w http.ResponseWriter, r *http.Request) { 
	params := mux.Vars(r)
	query := r.URL.Query()
	name := params["name"]
	remote := query.Get("remote")
	xRegistryAuth := r.Header.Get("X-Registry-Auth")
	body := &[]InlineObject{}
	if err := json.NewDecoder(r.Body).Decode(&body); err != nil {
		w.WriteHeader(http.StatusBadRequest)
		return
	}
	
	result, err := c.service.PluginUpgrade(r.Context(), name, remote, xRegistryAuth, *body)
	//If an error occured, encode the error with the status code
	if err != nil {
		EncodeJSONResponse(err.Error(), &result.Code, w)
		return
	}
	//If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
	
}
